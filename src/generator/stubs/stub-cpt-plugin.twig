<?php
/**
 * Plugin Name: {{ label }} Custom Post Type Plugin
 * Plugin URI:
 * Description: {{ description }}
 * Version: 0.1.0
 * Author:
 * Author URI:
 * License: GPL-3.0-or-later
 * Text Domain: {{ package_name }}
 * Domain Path: /languages
 * Update URI:
 *
 * @package {{ package_name }}
 */

declare(strict_types=1);

namespace {{ namespace }};

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

if ( class_exists( '\Queulat\Helpers\Autoloader' ) ) {
	\Queulat\Helpers\Autoloader::boot( '{{ namespace }}', __DIR__ . '/includes' );
}

add_action( 'plugins_loaded', '\{{ namespace }}\bootstrap' );

register_activation_hook( __FILE__, '\{{ namespace }}\plugin_activation' );

/**
 * Initialize plugin functionality
 */
function bootstrap() {
	$post_type = new Post_Type();
	$post_type->init();
}

/**
 * Run plugin activation hook
 */
function plugin_activation() {
	Post_Type::activate_plugin();
}
